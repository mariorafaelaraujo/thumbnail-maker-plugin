{
  "name": "thumbnail-maker-plugin",
  "version": "1.0.0",
  "description": "Backend auxiliar para GPTs que usam thumbnails do YouTube como referência visual.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check youtube.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
